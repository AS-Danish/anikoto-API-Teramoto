{
  "name": "luffytv-stream-proxy",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node proxy.test.mjs",
    "check": "wrangler deploy --dry-run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
